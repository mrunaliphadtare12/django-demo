# Django Demo — CRUD + External API + Chart

## What this project contains
- Django + Django REST Framework CRUD API for `Item`
- Example third-party integration (JSONPlaceholder)
- Simple Chart.js-based visualization served by Django
- Deployable to Render / other hosts, PostgreSQL on Supabase

## Quick start (locally)
1. Clone and create virtualenv:
```bash
git clone https://github.com/<you>/django-demo.git
cd django-demo
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
